export type VendorFormState = {
  name: string;
  description: string;
  address: string;
  imageUrl: string;
  latitude: string;
  longitude: string;
  isActive: boolean;
};

export type NewMenuItemFormState = {
  name: string;
  description: string;
  category: string;
  price: string;
};
