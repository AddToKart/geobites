PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=geobites

BETTER_AUTH_SECRET=geobites-dev-secret-change-in-production
BETTER_AUTH_URL=http://localhost:3000

CORS_ORIGIN=http://localhost:5173,http://localhost:8081,http://localhost:19006

# Database type: 'sqlite' (persistent file, no setup needed) or 'postgres'
# Defaults to 'postgres' if not set
DB_TYPE=postgres
